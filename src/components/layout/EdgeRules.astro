---
/**
 * The two full-height hairlines that frame the page.
 *
 * Fixed rather than absolute so they stay put while the page scrolls, which is
 * what makes them read as the page's margins rather than as part of any one
 * section. Styles live in src/styles/comic.css because the inset steps are
 * shared page chrome, not component state.
 */
---

<div class="edge-rules" aria-hidden="true"></div>
